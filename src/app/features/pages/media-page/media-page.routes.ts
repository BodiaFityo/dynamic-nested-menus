import {Route} from '@angular/router';
import {MediaPageComponent} from './components/media-page.component';

export const mediaPage: Route[] = [
    {
        path: '',
        component: MediaPageComponent,
    },
];

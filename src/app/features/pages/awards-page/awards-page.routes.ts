import {Route} from '@angular/router';
import {AwardsPageComponent} from './components/awards-page.component';

export const awardsPage: Route[] = [
    {
        path: '',
        component: AwardsPageComponent,
    },
];
